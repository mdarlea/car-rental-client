export class Configuration {
  apiBaseUrl: string;
  baseUrl: string;
  clientId: string;
  url: any;
  facebook: {
    appId: string
  };
  google: {
    clientId: string
  };
  foursquare: {
    clientId: string;
    clientSecret: string;
  };
}
